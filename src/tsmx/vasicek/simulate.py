import jax.numpy as jnp

def sim(alpha, beta, sigma, steps):
    """
    Simulate the Vasicek short rate model:
    dr = alpha(beta - r)dt + sigma dW
    """
    i = 0
    while (i < steps):
        



        i =+ 1
